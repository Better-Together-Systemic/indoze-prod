# ============================================================
# Variáveis de ambiente do INDOZE
# Copie para .env.local (desenvolvimento) e configure no Vercel (produção)
#
# ATENÇÃO: só entram aqui chaves PÚBLICAS (publishable/anon).
# A chave SECRETA (service_role / sb_secret_...) NUNCA vem para cá.
# Ela só existe no servidor (Supabase Edge Function).
# Tudo que começa com VITE_ vai junto para o navegador — é público.
# ============================================================

VITE_SUPABASE_URL=https://xknnytufqnopzgflcquc.supabase.co
VITE_SUPABASE_PUBLISHABLE_KEY=sb_publishable_COLE_A_CHAVE_PUBLICA_AQUI
VITE_INDEZ_PROXY_URL=https://xknnytufqnopzgflcquc.supabase.co/functions/v1/anthropic-proxy
